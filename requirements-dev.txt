-r requirements.txt

pytest==9.1.1
pytest-django==4.14.0
pytest-asyncio==1.4.0

# O --reload do uvicorn só honra --reload-include com o watchfiles instalado;
# sem ele, o reloader cai no modo que observa apenas *.py.
watchfiles==1.2.0

# Testes de navegador: e2e e comparação de telas. Ficam de fora do `pytest`
# pelado (veja pytest.ini) porque exigem o Chromium, que se instala com
# `playwright install chromium`.
pytest-playwright==0.9.0
pixelmatch==0.4.0
pillow==12.3.0
